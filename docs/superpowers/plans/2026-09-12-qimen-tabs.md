# 出生奇门与三盘切换实施计划

> 按已确认方案在当前会话执行。保留现有未提交修改，在现有功能分支上增量实现，不发布。

**目标：** 八字、紫微、奇门各自显示命盘及分析；出生奇门九宫可点击解释。

**架构：** 独立纯函数排盘引擎、独立解释数据、受控九宫组件；既有页面负责出生数据和选择状态。沿用 lunar-javascript，无新增运行依赖。出生标准时用于节气边界，已校正真太阳时用于日时干支；奇门明确使用子初换日，避免伪称与八字夜子时完全相同。

**已确认：** 首版出生时间起局；事件时间起局后续增加。时家转盘，甲己符头定三元，按实际节气定局，中宫寄坤；八神采用值符、腾蛇、太阴、六合、白虎、玄武、九地、九天。解释为文化结构参考，不包装为已验证的人生预测。

## 任务 1：排盘与解释
- [x] 新增 tests/qimen.test.mjs：阳五局庚申、阴三局辛亥、甲旬首、中宫寄干；日期非法拒绝、夏至边界、23点换日、九宫解读随输入变化。
- [x] 先运行测试确认缺少功能失败。
- [x] app/qimen-engine.ts：calculateQimen({date,time,standardDate,standardTime})，返回节气、符头、局数、四柱及九宫数组；layoutQimen(hourPillar,ju,yang) 供固定布局核验。
- [x] app/qimen-reading.ts：explainQimenPalace(chart,id)，返回本宫依据、门宫生克、符号含义、可核对建议；中宫明确寄宫去向。
- [x] 运行新测试确认通过，记录算法来源和限制。

## 任务 2：三盘独立视图
- [x] 新增 app/qimen-panel.tsx：受控选择九宫，显示南上北下方位、局数、门星神及天/地盘干；详情显示真实组合。
- [x] app/page.tsx：第三入口及可访问 tab 状态、对应 panel；八字解读及大运仅在八字页展示；紫微建议不依赖八字喜忌。
- [x] app/globals.css：九宫在移动端仍维持三列，详情纵向排列，焦点清楚。
- [x] 新增组件渲染测试，浏览器验证三入口切换、九宫点击、重新排盘更新与手机宽度。

## 任务 3：验证与交付
- [x] 类型检查、构建及既有自动测试；不调用收费模型。
- [x] 核对新增修改差异，保留已有未提交成果。
- [x] 启动本地预览供用户查看；报告实现范围和剩余事件起局任务。

最终验证：75 项测试通过，类型检查与构建通过；本地浏览器交互检查通过。保持当前分支及未提交工作，未推送部署。
